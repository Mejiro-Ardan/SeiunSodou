kind: pipeline
type: docker
name: default

steps:
  - name: backend
    image: python:3.12
    commands:
      - cd backend
      - pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
      - python main.py

trigger:
  branch:
    - master